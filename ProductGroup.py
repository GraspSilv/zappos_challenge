# ProductGroup.py
class ProductGroup:
	def __init__(self, num_items, max_spent):
		self.itemCount = num_items
		self.maxPrice = max_spent
		self.totalSpent = 0
		self.items = []

	def addOne(item):
		items.append(item)

	def printAllByKey(key):
		for item in items:
			print item[key]